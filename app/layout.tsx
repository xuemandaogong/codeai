import type { Metadata } from 'next'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  title: 'CodeAI - AI Coding Assistant',
  description: 'AI-powered coding assistant to generate, explain, fix, optimize, and convert code in your browser.',
  keywords: ['AI', 'coding', 'programming', 'assistant', 'code generator', 'code fixer'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className="min-h-screen flex flex-col">
        <Header />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
